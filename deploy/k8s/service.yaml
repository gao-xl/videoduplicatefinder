apiVersion: v1
kind: Service
metadata:
  name: vdf-web
  labels:
    app: vdf-web
spec:
  selector:
    app: vdf-web
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  type: ClusterIP
